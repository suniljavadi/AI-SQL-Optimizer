[
  {"id": 1, "sql": "SELECT * FROM orders WHERE YEAR(order_date)=2026", "expected": ["projection", "sargability"]},
  {"id": 2, "sql": "SELECT customer_id, COUNT(*) FROM orders GROUP BY customer_id", "expected": []},
  {"id": 3, "sql": "SELECT * FROM orders o JOIN customers c ON o.customer_id=c.customer_id", "expected": ["projection"]},
  {"id": 4, "sql": "SELECT DISTINCT region FROM customers", "expected": ["distinct"]},
  {"id": 5, "sql": "SELECT * FROM orders ORDER BY order_date DESC", "expected": ["projection", "sorting"]},
  {"id": 6, "sql": "SELECT * FROM customers", "expected": ["projection", "filtering"]},
  {"id": 7, "sql": "SELECT * FROM orders WHERE DATE_PART('year', order_date)=2026", "expected": ["projection", "sargability"]},
  {"id": 8, "sql": "SELECT status, SUM(total_amount) FROM orders GROUP BY status", "expected": []},
  {"id": 9, "sql": "SELECT * FROM orders WHERE customer_id=4", "expected": ["projection"]},
  {"id": 10, "sql": "SELECT * FROM orders o CROSS JOIN customers c", "expected": ["projection"]},
  {"id": 11, "sql": "SELECT * FROM orders o JOIN customers c", "expected": ["projection", "join"]},
  {"id": 12, "sql": "SELECT DISTINCT customer_id FROM orders", "expected": ["distinct"]},
  {"id": 13, "sql": "SELECT order_id, ROW_NUMBER() OVER (PARTITION BY customer_id ORDER BY order_date) FROM orders", "expected": []},
  {"id": 14, "sql": "WITH x AS (SELECT * FROM orders) SELECT * FROM x", "expected": ["projection"]},
  {"id": 15, "sql": "SELECT * FROM orders WHERE total_amount > 100", "expected": ["projection"]},
  {"id": 16, "sql": "SELECT c.region, COUNT(o.order_id) FROM customers c LEFT JOIN orders o ON o.customer_id=c.customer_id GROUP BY c.region", "expected": []},
  {"id": 17, "sql": "SELECT * FROM orders WHERE customer_id IN (SELECT customer_id FROM customers)", "expected": ["projection"]},
  {"id": 18, "sql": "SELECT * FROM orders WHERE order_date >= '2026-01-01' AND order_date < '2027-01-01'", "expected": ["projection"]},
  {"id": 19, "sql": "SELECT order_date, COUNT(*) FROM orders GROUP BY order_date ORDER BY order_date", "expected": ["sorting"]},
  {"id": 20, "sql": "SELECT * FROM customers WHERE region='East' ORDER BY name", "expected": ["projection", "sorting"]},
  {"id": 21, "sql": "SELECT AVG(total_amount) FROM orders", "expected": ["filtering"]},
  {"id": 22, "sql": "SELECT * FROM orders LIMIT 10", "expected": ["projection", "filtering"]},
  {"id": 23, "sql": "SELECT customer_id, MAX(order_date) FROM orders GROUP BY customer_id", "expected": []},
  {"id": 24, "sql": "SELECT * FROM orders WHERE LOWER(status)='shipped'", "expected": ["projection"]},
  {"id": 25, "sql": "SELECT * FROM orders o JOIN customers c ON CAST(o.customer_id AS TEXT)=c.customer_id", "expected": ["projection"]},
  {"id": 26, "sql": "SELECT DISTINCT c.region FROM customers c JOIN orders o ON c.customer_id=o.customer_id", "expected": ["distinct"]},
  {"id": 27, "sql": "SELECT * FROM orders WHERE order_date BETWEEN '2026-01-01' AND '2026-12-31'", "expected": ["projection"]},
  {"id": 28, "sql": "SELECT customer_id, SUM(total_amount) OVER (PARTITION BY customer_id) FROM orders", "expected": []},
  {"id": 29, "sql": "SELECT * FROM orders WHERE EXISTS (SELECT 1 FROM customers c WHERE c.customer_id=orders.customer_id)", "expected": ["projection"]},
  {"id": 30, "sql": "SELECT * FROM orders ORDER BY total_amount DESC LIMIT 20", "expected": ["projection", "sorting"]},
  {"id": 31, "sql": "SELECT region, COUNT(*) FROM customers GROUP BY region HAVING COUNT(*) > 1", "expected": []},
  {"id": 32, "sql": "SELECT * FROM orders WHERE EXTRACT(YEAR FROM order_date)=2026", "expected": ["projection"]},
  {"id": 33, "sql": "SELECT o.* FROM orders o WHERE o.status='pending'", "expected": []},
  {"id": 34, "sql": "SELECT * FROM orders o INNER JOIN customers c ON o.customer_id=c.customer_id WHERE c.region='West'", "expected": ["projection"]},
  {"id": 35, "sql": "SELECT COUNT(DISTINCT customer_id) FROM orders", "expected": ["distinct"]},
  {"id": 36, "sql": "SELECT * FROM orders WHERE order_date >= CURRENT_DATE - INTERVAL '30 days'", "expected": ["projection"]},
  {"id": 37, "sql": "SELECT customer_id FROM orders UNION SELECT customer_id FROM customers", "expected": []},
  {"id": 38, "sql": "SELECT * FROM orders WHERE customer_id = '4'", "expected": ["projection"]},
  {"id": 39, "sql": "SELECT * FROM orders o LEFT JOIN customers c ON TRUE", "expected": ["projection"]},
  {"id": 40, "sql": "SELECT status, COUNT(*) FROM orders WHERE order_date >= '2026-01-01' GROUP BY status", "expected": []}
]
